#!/bin/bash

source _konix_sync_variables.sh

echo "PULL FROM (AND PUSH TO) : "
cat "$KONIX_SYNC_MAIN_DIR_FILE"
if [ -e "${KONIX_SYNC_DIR_FILE}" ]
then
    echo "PUSH TO :"
    cat -n "$KONIX_SYNC_DIR_FILE"
fi
