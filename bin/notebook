#!/bin/bash

set -eu
echo "Running in virtual env ${KONIX_NOTEBOOK_VIRTUALENV}"
set +u
source ~/.virtualenvs/${KONIX_NOTEBOOK_VIRTUALENV}/bin/activate
jupyter notebook --browser ${KONIX_NOTEBOOK_BROWSER} --NotebookApp.iopub_data_rate_limit=10000000000 --ip 0.0.0.0 "$@"
