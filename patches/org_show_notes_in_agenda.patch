Provide a way to add my custom notes in the agenda view when in log modeIndex: devel/config/elfiles/org/lisp/org-agenda.el
===================================================================
--- devel.orig/config/elfiles/org/lisp/org-agenda.el	2013-01-15 10:54:46.429978670 +0100
+++ devel/config/elfiles/org/lisp/org-agenda.el	2013-01-15 10:55:25.105978301 +0100
@@ -5737,6 +5737,7 @@
 	 (parts
 	  (delq nil
 		(list
+		 "- Note prise le "
 		 (if (memq 'closed items) (concat "\\<" org-closed-string))
 		 (if (memq 'clock items) (concat "\\<" org-clock-string))
 		 (if (memq 'state items) "- State \"\\([a-zA-Z0-9]+\\)\".*?"))))
